# Dev Notes

Random things to remember.

## Quick fixes we could do
- Maybe simplify validation.js later if we want
- Consider using a library like Zod instead of custom validation
  - Tried and tested in real-world environments, making it safer and more reliable for large scale production

## Database
Current table name is `login` which is weird - should be `users`, works for now.
Any db changes can be made in loop 2 when other stuff is added

## To do next
- User dashboard
- Logout button
- UI
- Start looking at recommender and multilanguage support

