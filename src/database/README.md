# src/database — Database assets

This directory (`/src/database`) is for database-related files and configurations.

- Database schema, migration scripts, and seed data
- ORM models or database client setup (server-only)
- Used by server-side code to interact with the database

For project-wide docs and SSR/CSR guidance, see: ../../README.md
