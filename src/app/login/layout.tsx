export const metadata = {
  title: "Sign In",
  description:
    "Sign in to OghmaNotes to access your notes, quizzes, and AI study tools.",
};

import type { ReactNode } from "react";

export default function LoginLayout({ children }: { children: ReactNode }) {
  return children;
}
