# Superseded local secret notes

This tracked file previously contained workstation-specific paths and password
manager procedures. They do not belong in a shared application runbook.

The current repository policy, secret boundaries, rotation guidance, and link
to the private operational instructions live in
[the operations secrets policy](../operations/secrets.md). Keep local paths,
vault item names, and recovery instructions in the private `server-stacks`
documentation instead of this repository.
